
extends layout

block content
  div
    | This is a list of all the files on either your local
    | node instance, or when running on heroku in live dyno
    | tmp folder.
    
  table.table.table-striped
    thead
      tr
        th Image
        th Local Name
        th S3 Name

    tbody
      - for (var i = 0; i < files.length; ++i)
        tr
         td
          img(class='img-polaroid', width='50', src='/images/' + files[i])
          &nbsp;
         td
          = files[i]
         td
          | URL
         td 
           button(class="btn btn-small", type="button", onclick="document.location='/local/delete/" + files[i] + "';") Remove
